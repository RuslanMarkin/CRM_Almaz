# SDLC handoff: CRM-25

**Бизнес-задача:** [[Бизнес] Убрать столбец договоров из списка контрагентов](https://github.com/RuslanMarkin/CRM_Almaz/issues/25)

Этот файл автоматически создан после human approval. На данном этапе ветка не содержит продуктовых изменений: реализация должна выполняться только в этом draft PR и проходить обычный GitHub review.

## Утверждённая аналитическая спецификация

```json
{
  "summary": "На экране «Контрагенты» (client/src/pages/Counterparties.tsx) требуется полностью удалить столбец «Договоров» из таблицы списка контрагентов: заголовок столбца, отображение числовых значений количества договоров в строках и связанную с этим логику отображения. Остальные столбцы, поиск, создание/редактирование/удаление контрагентов, состояния загрузки и пустого списка должны работать без изменений. В предоставленных фрагментах репозитория столбец «Договоров» в шапке таблицы списка не виден (видимая шапка: Наименование, Тип, Статус, ИНН, Телефон, Добавлен), а тело таблицы и server/db.ts обрезаны, поэтому точное место рендеринга и источник данных столбца требуют подтверждения аналитиком.",
  "scope": [
    "Удалить заголовок столбца «Договоров» из <thead> таблицы на странице client/src/pages/Counterparties.tsx (включая css-классы адаптивности вида hidden md/lg/xl:table-cell, если они привязаны к этому столбцу).",
    "Удалить рендеринг ячеек с количеством договоров в <tbody> таблицы списка контрагентов (включая slice/подсчёт/выражение, формирующее число).",
    "Удалить клиентскую логику/переменные/импорты, использовавшиеся только для отображения столбца «Договоров» в списке (например, обращение к полю счётчика договоров в объекте строки списка).",
    "Проверить и, если поле счётчика договоров в ответе counterparties.list используется только этим столбцом, исключить его вычисление/возврат в server/db.ts (getCounterparties) и соответствующем участке server/routers.ts, чтобы не осталось мёртвой логики.",
    "Провести поиск по репозиторию (например, по имени поля счётчика договоров) для выявления других потребителей удаляемого поля перед его исключением из API-ответа."
  ],
  "non_goals": [
    "Не менять модуль договоров, структуру таблицы contracts в drizzle/schema.ts и API CRUD договоров в server/routers.ts.",
    "Не трогать блок «Договоры» в карточке контрагента (client/src/pages/CounterpartyDetail.tsx) и переходы на /contracts?counterpartyId=...",
    "Не добавлять новые столбцы, фильтры, сортировку или иной редизайн таблицы контрагентов.",
    "Не менять логику поиска, счётчик «N записей в базе», дашбордную статистику (dashboard.stats) и сценарии создания/редактирования/удаления контрагентов.",
    "Не удалять и не изменять данные о договорах в БД."
  ],
  "acceptance_criteria": [
    "В таблице списка контрагентов на экране «Контрагенты» отсутствует ячейка заголовка с текстом «Договоров».",
    "Ни в одной строке таблицы списка контрагентов не отображается количество договоров контрагента.",
    "В client/src/pages/Counterparties.tsx не осталось неиспользуемых переменных, импортов или обработчиков, связанных со столбцом «Договоров»; сборка типов проходит без ошибок (pnpm check / tsc --noEmit).",
    "Поиск по списку контрагентов, открытие формы создания, редактирование и удаление контрагента работают как до изменения.",
    "Состояние загрузки («Загрузка...») и состояние пустого списка («Контрагенты не найдены») отображаются корректно и без изменений.",
    "Счётчик «N записей в базе» и остальные столбцы (Наименование, Тип, Статус, ИНН, Телефон, Добавлен) отображаются без изменений и без сдвига/поломки разметки таблицы.",
    "Если поле счётчика договоров исключено из ответа counterparties.list, экран-потребитель этого ответа не падает и не отображает пустых/undefined-значений, а модуль договоров продолжает работать."
  ],
  "affected_files": [
    "client/src/pages/Counterparties.tsx",
    "server/db.ts",
    "server/routers.ts",
    "shared/types.ts",
    "drizzle/schema.ts"
  ],
  "risks_and_questions": [
    "В предоставленном фрагменте Counterparties.tsx шапка таблицы не содержит столбца «Договоров», а тело таблицы обрезано (обрывается на 'hover:b...'), server/db.ts не предоставлен: не подтверждено точное место рендеринга столбца и источник значения. Требуется подтверждение аналитика/разработчика, где именно находится удаляемая логика.",
    "Неизвестно, откуда берётся количество договоров: агрегируется в getCounterparties (server/db.ts) или вычисляется отдельным запросом/иным способом на клиенте. Это влияет на объём изменений и на возможность удаления серверной логики.",
    "Не выяснено, используется ли поле счётчика договоров где-либо ещё (дашборд, отчёты, другие страницы). Удаление поля из API без анализа влияния может сломать других потребителей — требуется grep/impact-анализ.",
    "Бизнес-правило о необходимости сортировки/фильтрации по количеству договоров в задаче не описано; по умолчанию считается, что такие сценарии не требуются, но это следует подтвердить.",
    "Требования к тестам (vitest) для данного UI-изменения в задаче отсутствуют; необходимо уточнить, требуется ли добавить/обновить автотест.",
    "Не указано, нужно ли сохранять обратную совместимость ответа counterparties.list (удалять поле сразу или помечать устаревшим)."
  ]
}
```

## Утверждённый план разработки

```json
{
  "implementation_steps": [
    "Провести impact-анализ: выполнить поиск по репозиторию по имени поля счётчика договоров (например, grep -rn \"contractCount\\|contractsCount\\|contract_count\\|contracts.length\" client/src server shared drizzle) и зафиксировать всех потребителей поля, чтобы убедиться, что столбец «Договоров» используется только на странице списка контрагентов.",
    "Подтвердить источник данных количества договоров: определить, агрегируется ли значение в getCounterparties (server/db.ts) через JOIN/подсчёт по таблице contracts, вычисляется ли отдельным запросом в server/routers.ts, либо приходит как поле строки списка в shared/types.ts.",
    "В client/src/pages/Counterparties.tsx удалить элемент <th> столбца «Договоров» из <thead> таблицы списка, включая привязанные к этому столбцу адаптивные классы (hidden md/lg/xl:table-cell), если они относятся только к нему.",
    "В client/src/pages/Counterparties.tsx удалить рендеринг <td> с количеством договоров в <tbody> (включая slice/подсчёт/выражение, формирующее число) и синхронно скорректировать colspan/разметку строки загрузки и строки пустого состояния, если они зависят от числа столбцов.",
    "В client/src/pages/Counterparties.tsx удалить ставшие неиспользуемыми переменные, вычисления, импорты и/или обращения к полю счётчика договоров в маппинге строк списка.",
    "Если подтверждено, что поле счётчика договоров используется только удаляемым столбцом: исключить его вычисление/возврат в getCounterparties (server/db.ts) и в соответствующем участке server/routers.ts; при наличии — убрать поле из типа строки списка в shared/types.ts. Если найдены другие потребители — оставить поле на сервере без изменений и ограничиться только UI.",
    "Убедиться, что логика поиска, счётчик «N записей в базе», состояния загрузки и пустого списка, а также создание/редактирование/удаление контрагентов не затронуты изменениями.",
    "Прогнать проверку типов и линтер, затем вручную проверить рендер таблицы (отсутствие сдвига разметки, корректные заголовки Наименование, Тип, Статус, ИНН, Телефон, Добавлен)."
  ],
  "files_to_change": [
    "client/src/pages/Counterparties.tsx",
    "server/db.ts",
    "server/routers.ts",
    "shared/types.ts"
  ],
  "migration_plan": "Изменение чисто презентационное: миграции схемы и данных не требуются, структура таблицы contracts в drizzle/schema.ts и данные о договорах не изменяются. Серверная часть (server/db.ts, server/routers.ts, shared/types.ts) затрагивается только при подтверждённом impact-анализом отсутствии иных потребителей поля счётчика договоров, и в этом случае поле просто исключается из выборки/ответа counterparties.list без изменения хранимых данных. Обратная совместимость ответа API сохраняется: если поле остаётся, клиент его игнорирует; удаление поля помечается в PR как потенциально ломающее и подтверждается grep-анализом по всем потребителям. Откат — revert коммита, поскольку изменения не затрагивают БД.",
  "verification_commands": [
    "pnpm check",
    "pnpm tsc --noEmit",
    "pnpm lint",
    "grep -rn \"contractCount\\|contractsCount\\|contract_count\" client/src server shared drizzle",
    "pnpm test",
    "pnpm dev"
  ]
}
```

## Утверждённый QA-план

```json
{
  "automated_tests": [
    "Статическая проверка (tsc --noEmit / pnpm check): проект собирается без ошибок типов после удаления столбца «Договоров» — отсутствуют неиспользуемые переменные, импорты и обработчики, связанные со столбцом.",
    "Snapshot/RTL-тест рендера Counterparties.tsx с мок-данными списка контрагентов: в шапке таблицы отсутствует ячейка (th) с текстом «Договоров» (проверка по всем ячейкам заголовка и по отсутствию текста/роли columnheader «Договоров»).",
    "RTL-тест строк таблицы: для каждого контрагента в <tbody> отсутствует ячейка с числовым значением количества договоров; количество td в строке соответствует набору оставшихся столбцов (Наименование, Тип, Статус, ИНН, Телефон, Добавлен).",
    "RTL-тест на согласованность количества столбцов: число th в <thead> равно числу td в каждой строке <tbody> (нет разъезда разметки/сдвига колонок).",
    "RTL-тест поиска: при вводе запроса список фильтруется как раньше, отфильтрованные строки также не содержат ячейки/значения количества договоров.",
    "RTL-тест состояния загрузки: при isLoading=true отображается «Загрузка...» и таблица со строками не рендерится.",
    "RTL-тест пустого состояния: при пустом отфильтрованном списке отображается «Контрагенты не найдены» и отсутствует <tbody> со строками.",
    "RTL-тест счётчика «N записей в базе»: значение счётчика корректно вычисляется и отображается (не зависит от удалённого столбца).",
    "RTL-тест остальных столбцов: заголовки и значения «Наименование», «Тип», «Статус», «ИНН», «Телефон», «Добавлен» рендерятся без изменений относительно эталона.",
    "Тест на отсутствие мёртвой клиентской логики: отсутствуют обращения к полю счётчика договоров в компоненте списка (проверка через grep/статический анализ или через мок-объект списка без поля — компонент не должен обращаться/падать).",
    "Серверный тест counterparties.list (если поле счётчика удалено из ответа): ответ содержит ожидаемый набор полей без поля количества договоров, схема ответа валидна, запрос выполняется без ошибок.",
    "Серверный тест обратной совместимости (если поле сохранено): getCounterparties возвращает поле количества договоров (или возвращает корректный тип/undefined) — фиксирует принятое решение и не ломает потребителей.",
    "Регрессионный тест модуля договоров: contracts CRUD (list/create/update/delete) в server/routers.ts работает как раньше, независимо от изменения списка контрагентов."
  ],
  "manual_staging_checks": [
    "Открыть экран «Контрагенты» на staging: визуально подтвердить, что в шапке таблицы нет столбца «Договоров», а заголовки Наименование, Тип, Статус, ИНН, Телефон, Добавлен отображаются корректно и без сдвига.",
    "Прокрутить список контрагентов: ни в одной строке не отображается число договоров; колонки выровнены, горизонтальный скролл/переносы не сломаны.",
    "Проверить адаптивность на разных ширинах (мобильная/планшет/десктоп): таблица корректна, нет пустых/сломанных ячеек и зарезервированного под удалённый столбец места.",
    "Ввести поисковый запрос и убедиться, что фильтрация работает, счётчик «N записей в базе» отображается корректно, найденные строки без столбца «Договоров».",
    "Очистить поиск и получить пустой результат: убедиться, что отображается «Контрагенты не найдены» и нет ошибок в консоли браузера.",
    "Создать нового контрагента: форма открывается, создание проходит успешно, новая строка появляется без столбца «Договоров».",
    "Отредактировать существующего контрагента и сохранить: изменения применяются, таблица и остальные столбцы отображаются корректно.",
    "Удалить контрагента: удаление проходит, список и счётчик «N записей в базе» обновляются корректно.",
    "Открыть карточку контрагента (CounterpartyDetail.tsx) и блок «Договоры»: блок и переходы на /contracts?counterpartyId=... работают без изменений.",
    "Проверить вместо/в коде ответа counterparties.list (DevTools Network): подтвердить наличие/отсутствие поля счётчика договоров и что клиент не рендерит undefined/пустых значений.",
    "Проверить дашборд и страницу договоров: статистика dashboard.stats и список договоров не изменились, данные о договорах в БД не затронуты.",
    "Проверить консоль браузера и логи сервера при навигации по экрану «Контрагенты»: нет ошибок, warning об отсутствующих полях и ошибок рендера."
  ],
  "regression_risks": [
    "Удаление серверного поля счётчика договоров из ответа counterparties.list может сломать других потребителей (дашборд, отчёты, другие экраны), если поле использовалось вне столбца таблицы — требуется impact-анализ/grep по имени поля.",
    "Возможно расхождение числа столбцов <thead> и <td> в строках, приводящее к сдвигу таблицы или неверному выравниванию заголовков, особенно на адаптивных брейкпоинтах (hidden md/lg/xl:table-cell).",
    "Неточное удаление только заголовка/только ячеек может оставить неиспользуемые переменные, импорты или вычисление счётчика договоров — риск падения сборки типов (pnpm check / tsc --noEmit).",
    "Если количество договоров вычислялось агрегирующим запросом в getCounterparties, его удаление может повлиять на производительность/поведение запроса и на сортировку или пагинацию списка, если она от него зависела.",
    "Возможное влияние на поиск, счётчик «N записей в базе», состояния загрузки и пустого списка при правке разметки таблицы и логики строк.",
    "Обратная совместимость API: если поле счётчика удалено, старые клиентские или кэшированные версии могут отображать пустые/undefined-значения — риск рассинхрона версий front/back.",
    "Точное место рендеринга столбца «Договоров» не подтверждено предоставленными фрагментами (шапка без столбца, тело и server/db.ts обрезаны) — есть риск изменить не тот участок или пропустить источник данных.",
    "Непроверенные сценарии сортировки/фильтрации по количеству договоров: если такие сценарии требуются, их удаление/изменение может дать регресс функциональности."
  ]
}
```
